TRIGGER "SET_VERSION_TEST"
BEFORE UPDATE ON "DB_TESTDATA" 
REFERENCING NEW ROW MYNEWROW
FOR EACH ROW
BEGIN
    DECLARE vers INTEGER;
    SELECT DATA_VERSION_SEQUENCE.NEXTVAL INTO vers FROM DUMMY;
    MYNEWROW.VERSION = :vers;
END
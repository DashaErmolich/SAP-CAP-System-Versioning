module.exports = function (srv) {
    const { MasterData, DataHistory } = srv.entities;

    // this.on('READ', '*', async () => {
    //     // await DELETE.from(Data);
    //     // await DELETE.from(DataVersions);
    //     await DELETE.from(MasterData);
    //     await DELETE.from(DataHistory);
    // })

}